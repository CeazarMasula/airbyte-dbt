
version: 2

models:
    - name: memberbetrecordnexts
      description: "member bet record model"
      columns:
          - name: _id
            description: "The primary key for this table"
          - name: turnover
            description: "turnover"
            data_type: numeric
              
